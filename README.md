# Claude Code 新手入門包（Mac 版）

給第一次接觸 AI 工具的你。這份指南帶你裝好 Claude 桌面版 app，
然後——這是最棒的部分——**讓 AI 自己幫你完成剩下的所有設定**。
你要做的只有：下載、登入、貼一段話。

---

## Claude Code 是什麼？

你可能用過 ChatGPT：像一位「用訊息聊天的顧問」，你問它答。

Claude Code 不一樣，它像**坐在你電腦前的助理**：你用日常語言交代事情，
它可以直接幫你讀寫檔案、整理資料夾、上網查資料、做簡報、查醫學文獻。
全程不需要會寫程式。

## 你需要準備

- 一台 Mac（macOS 13 以上）
- **Claude 付費帳號**：到 [claude.ai](https://claude.ai) 註冊並訂閱 Pro（約 US$20／月）或 Max。
  免費帳號不能用 Claude Code。
- 大約 30–60 分鐘

---

## 第 1 步：安裝 Claude 桌面版

1. 到官方下載頁 [claude.ai/download](https://claude.ai/download)，下載 macOS 版。
2. 打開下載好的檔案（.dmg），把 Claude 圖示**拖進「應用程式（Applications）」資料夾**。
3. 從「應用程式」打開 Claude，用你的 Claude 帳號登入。

就這樣——跟裝其他 Mac 軟體一樣，不需要打任何指令。

## 第 2 步：進入 Code 分頁、選一個工作資料夾

1. 在 app 裡點 **Code** 分頁——這就是 Claude Code，「會動手做事」的那個模式。
   （如果這裡跳出要你升級方案，代表帳號還沒訂閱 Pro，先回第 0 步。）
2. 它會請你選一個資料夾當工作區：
   先去「文件」裡**建一個新資料夾，取名 `claude-work`**，然後選它。
   以後 Claude 都在這個資料夾裡幫你做事。

## 第 3 步：把剩下的設定交給 AI（魔法咒語）

把下面這**整段**複製，貼進 Claude 的輸入框，按 Enter：

```
請先讀取這個網址的設定指南：
https://raw.githubusercontent.com/Dhahran123/claude-code-starter-tw/main/SETUP.md
讀完之後，照著裡面的指示一步一步帶我完成所有設定。
我是完全新手、不是工程師，用的是 Claude 桌面版 app，
請全程用白話的繁體中文，每一步先告訴我會發生什麼事再動手。
```

Claude 動手前都會跳出視窗徵求你的同意（允許／拒絕的按鈕）。
看得懂就允許；看不懂就先問它「這一步在做什麼？」——它會解釋，不會不耐煩。

接下來大約 20–40 分鐘，它會陸續幫你：

1. **建立你的個人設定檔**（它會先訪問你幾個問題，把你的偏好寫成它的「員工手冊」）
2. **接上外部工具**：操作瀏覽器、讀網頁、查 PubMed 醫學文獻資料庫
3. **安裝技能包**：文書處理（Word／PPT／Excel／PDF）、牙科文獻檢索與評讀、網頁簡報
4. **帶你做幾個小測試**，教你日常怎麼用
5. **選配**：接上 Gmail 與 Google 行事曆、設定第二位 AI（Codex）互審——要不要都由你決定

---

## 之後平常怎麼用

每次要用：打開 Claude app → **Code** 分頁 → 開始交代事情。

| 你想做的事 | 怎麼做 |
|---|---|
| 交代任務 | 在輸入框用日常語言打字，像跟助理說話 |
| 回到之前的對話 | 左側清單直接點那個對話 |
| 對話太長、變慢了 | 輸入 `/compact` 幫對話瘦身 |
| 換一個全新話題 | 開一個新對話（或輸入 `/clear`） |
| 看有哪些指令 | 輸入 `/help` |
| 它做到一半你想喊停 | 按 `Esc` |

**新手心法：**

- 描述「你要的結果」就好，不用管步驟。
  例：「幫我把桌面那份 PDF 的重點整理成一頁 Word。」
- 它看得到你電腦裡的檔案（你允許的範圍內），所以可以直接說「桌面那個資料夾」。
- 做錯了就叫它改，檔案改壞了就叫它還原，它不會覺得煩。
- 不知道怎麼開口，就先問：「我想做◯◯，你會建議怎麼做？」

---

## 還能更進一步（都是選配，設定時 AI 會問你要不要）

- **信箱與行事曆**：接上 Gmail 和 Google 日曆後，可以直接問
  「我明天有什麼行程？」「找◯◯寄來的那封信」「幫我加一個下週二的會議」。
  設定超簡單：點兩下選單、用 Google 帳號登入授權就好，AI 會帶著你。
- **第二位 AI 互審（Codex）**：如果你也有訂閱 ChatGPT，可以加裝 OpenAI 的
  Codex 當「第二意見」——重要的東西做完，讓兩家公司的 AI 互相挑錯再交出去。

當下跳過了也沒關係，之後隨時跟 Claude 說「我想接行事曆」「我想裝 Codex」就能補裝。

---

## 想用終端機版？（進階，可跳過）

Claude Code 也有純文字的「終端機版」，適合之後想玩自動化排程的人。
打開「終端機」（Cmd＋空白鍵搜尋 Terminal），貼這行安裝：

```
curl -fsSL https://claude.ai/install.sh | bash
```

裝完重開終端機、輸入 `claude` 登入即可。
桌面版與終端機版**共用同一套設定與技能**，兩邊可以並用、互不干擾。

---

## ⚠️ 給醫療人員的重要提醒

**病人的個資——姓名、病歷、口內照、X 光、約診資料——不要餵給任何雲端服務。**

Claude Code 在「你自己的電腦裡」讀寫檔案沒有問題；
但只要牽涉「傳出去」（上傳到生圖網站、公開發布、外部表單），
就必須先**去識別化**：拿掉姓名與所有可辨識資訊。

設定過程會把這條紅線寫進你的個人設定檔，之後 Claude 會自動幫你把關——
但最後一道防線永遠是你自己。

---

*這份入門包由劉哲銘醫師的 AI 工作環境整理而來。
有問題直接問你的 Claude，它比任何說明書都有耐心。*
